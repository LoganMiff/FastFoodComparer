import React from 'react';

const runQuery = () => {
    const response = "r";
}

const FFQueryButton = () => {
    return (<button onClick={() => runQuery()}>COMPARE</button>);
}

export default FFQueryButton;